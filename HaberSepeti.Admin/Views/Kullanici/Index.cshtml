@using PagedList.Mvc;
@model PagedList.IPagedList<HaberSepeti.Data.Model.Kullanici>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-12">
    <div class="panel panel-default">
        <header class="panel-heading">@ViewBag.Title</header>
        <div class="panel-body">
            @if (TempData["Bilgi"] != null)
            {
                <div class="alert alert-success">@TempData["Bilgi"]</div>
            }
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Ad Soyad</th>
                            <th>Email</th>
                            <th>Rol</th>
                            <th>Kayıt Tarihi</th>
                            <th>Durum</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><span class="label label-success">@item.Id</span></td>
                                <td>@item.AdSoyad</td>
                                <td> @item.Email</td>
                                <td> @item.Rol.Ad</td>
                                <td> @item.KayitTarihi.ToString("dd MM yyyy")</td>
                                <td>
                                    @if (item.AktifMi == true)
                                    {
                                        <b class="label label-success">Aktif</b>
                                    }
                                    else
                                    {
                                        <b class="label label-danger">Pasif</b>
                                    }
                                </td>
                                <td>
                                    @if (item.RolId == 3)
                                    {
                                        <a href="@Url.Action("MakeAdmin","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Admin yap</a>
                                        <a href="@Url.Action("MakeEditor","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Editör yap</a>
                                        <a href="@Url.Action("MakeAuthor","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Yazar yap</a>
                                    }
                                    else if (item.RolId == 2)
                                    {
                                        <a href="@Url.Action("MakeAdmin","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Admin yap</a>
                                    }
                                    else if (item.RolId == 4)
                                    {
                                        <a href="@Url.Action("MakeAdmin","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Admin yap</a>
                                        <a href="@Url.Action("MakeEditor","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Editör yap</a>
                                    }
                                    @if (item.RolId != 3)
                                    {
                                        <a href="@Url.Action("MakeMember","Kullanici",new { Id=item.Id})" class="btn btn-default btn-sm">Yetki al</a>
                                    }
                                    <a href="@Url.Action("Sil","Kullanici", new { Id=item.Id })" class="btn btn-default btn-sm">Sil</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            @Html.PagedListPager(Model, sayfa => Url.Action("Index", new { sayfa }), PagedListRenderOptions.Classic)
        </div>
    </div>
</div>