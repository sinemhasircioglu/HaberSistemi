@using PagedList.Mvc;
@model PagedList.IPagedList<HaberSepeti.Data.Model.Kategori>
@{
    ViewBag.Title = "Kategori Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<div class="col-md-12">
    <div class="panel panel-default">
        <header class="panel-heading">@ViewBag.Title</header>
        <div class="panel-body">
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Kategori Adı</th>
                            <th>Üst Kategori</th>
                            <th>Durum</th>
                            <th>Ekleyen</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><span class="label label-success">@item.Id</span></td>
                                <td>@item.Ad</td>
                                <td>@item.ParentId</td>
                                <td>
                                    @if (item.AktifMi == true)
                                    {
                                        <b class="label label-success">Aktif</b>
                                    }
                                    else
                                    {
                                        <b class="label label-danger">Pasif</b>
                                    }
                                </td>
                                <td><a href="#">@item.KullaniciId</a></td>
                                <td>
                                    <a href="@Url.Action("Duzenle","Kategori",new { Id=item.Id})" class="btn btn-default btn-sm">Düzenle</a>
                                    <button class="btn btn-default btn-sm" id="KategoriDelete" onclick="KategoriSil();" data-id="@item.Id">Sil</button>
                                </td> 
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            @Html.PagedListPager(Model , sayfa => Url.Action("Index",new { sayfa }),PagedListRenderOptions.Classic)
        </div>
    </div>
</div>
<script src="~/Scripts/Global.js"></script>
